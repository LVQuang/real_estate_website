spring:
  application:
    name: real_estate_website

  datasource:
    url: jdbc:mysql://localhost:3306/real_estate_website
    driverClassName: "com.mysql.cj.jdbc.Driver"
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
